# TrackedVehicle 项目协作约定

本文件记录项目长期约定，供后续 Agent 会话使用。用户在当前任务中的明确要求优先；不要把这里的偏好当作阻止用户修改设计的理由。

## 开发方式

- 优先简单、直观、方便人工维护的实现，按明确需求逐步完成。
- 不为尚未提出的功能提前搭框架；不顺带进行无关重构。
- 优先普通类、显式构造函数和清楚的 if/foreach。没有实际收益时，不引入 record、通用委托封装、多层抽象或复杂 Session 机制。
- 用户只询问代码含义时，先解释；用户要求修改时，直接完成所需修改，不额外扩展范围。
- 修改前阅读当前文件并检查已有改动，保留用户手写的代码和注释，不依赖旧会话中的代码版本。

## 命名与注释

- 用中文沟通和编写说明性注释，解释时结合本项目代码和具体例子。
- 相机 SDK 返回的整数编号统一称为 camId，与 C++ 接口保持一致；不要与配置中的字符串业务 Id 混淆。
- 尊重用户已确定的命名，例如 cameraDitc、LastStartedTimestamp，不在无关修改中自行改名。
- 对线程同步、取消、计时、回调和资源释放，注释说明用途、执行顺序与必要原因。
- 使用 SemaphoreSlim、Interlocked、Stopwatch 等 API 时，说明关键参数、并发行为和时间单位。不要仅复述代码字面意思。
- 不通过删除必要的并发保护来换取表面上的简短。

## 设备与识别流程

- PLC 通信、打开相机、初始化识别模型分别启动，互不等待；某一项失败不应连带停止其他功能。
- 实际调用 RobotX_LaneDetect 前必须成功初始化模型，并保证对应相机在调用期间保持打开。
- 相机帧回调只提交最新通知，不直接识别、不等待识别，也不按每帧创建后台任务。
- 识别间隔可配置，按同一相机两次开始识别的时间计算。没有新通知不识别，来不及处理时覆盖旧通知，不无限积压。
- 保留关闭相机前等待正在执行的识别结束的保护；CancellationToken 不能强行中断已经进入的原生调用。
- 帧回调编号只是触发信息：LaneDetect 不接收 frameIndex，不把它描述为实际识别帧的精确编号。
- SDK 的线程安全、缓存帧选择和关闭后回调行为，应以头文件、实现或设备验证为依据，不猜测契约。
- 矩形结果发送 PLC 属于后续独立需求，在用户提出前不自行接入或猜测报文格式。

## 验证与交付

- 当前不维护独立 tests 项目；不要未经用户要求重新创建测试工程或模拟测试框架。
- C# 逻辑或项目配置修改后，优先运行 `dotnet build TrackedVehicle.slnx --no-restore` 检查编译；缺少依赖时据实处理。
- 仅注释、文档或简单重命名修改，做相应检查即可，不扩大验证范围。
- 编译成功不等于设备联调成功。真实相机、PLC、Linux ARM64 原生库未验证的部分，交付时明确说明。
- 完成后简要说明修改内容和验证结果，不把日志或代码大段重复给用户。

## 项目资料

- `Core/VehicleControlCenter.cs`：PLC、相机和模型识别的启动与停止。
- `Core/Impl/CameraManager.cs`：相机 SDK 调用与回调。
- `Core/DetectionScheduler.cs`：最新通知、识别间隔与串行调度。
- `Core/Impl/DetectManager.cs`：模型初始化和识别接口封装。
- `Core/Detection.md`：识别配置和运行流程说明。

本文件保存稳定约定，不充当完整聊天记录。需求变更时同步修订相关条目。
